import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  container: {
    backgroundColor: "rgba(248, 215, 152, 0.65)",
  },
  logo: {
    height: 26.63,
    width: "auto",
  }
}));
