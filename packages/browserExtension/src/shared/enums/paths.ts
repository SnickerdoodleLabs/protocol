export enum EPATHS {
  HOME = "/",
  LOGIN = "/",
  WELCOME = "/welcome",
  ACCOUNT_DETAILS = "/account-details",
  SETTINGS = "/settings",
  NOTIFICATIONS = "/notifications",
  TRANSACTION = "/transaction",
  HISTORY = "/history",
}
