class IAdService {


    // A set of functions to be used by Query Parsing Engine

    // saveAd(eligibleAd)
    // getAdSignatures(queryCID) returns a list of signatures.

    // A set of functions to display and track ad engagement
    // getEligibleAds()
    // requestDisplay(eligigleAd)

    // event subscriptions
    // onAdDisplayed(queryCID, adId, contentHash)
    
    // createSignature(queryCID, adId, contentHash) returns AdSignature(queryCID, adId, signature)
    // saveSignature(adSignature) 

    // A set of functions to anything else
    
}