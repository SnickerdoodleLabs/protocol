name: Extension Build Prod

on:
  push:
    branches: main

jobs:
  build_extension:
    name: Setup, Build, and Publish
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v3
      - name: scrapping
        run: |-
          python infra/scrapper/coin_list_gen.py
          mv data.json packages/indexers/coinList.json      
      - name: prices
        run: |-
          python infra/prices/coin_price_gen.py
          mv data.json packages/indexers/coinPrices.json
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - run: |-
          cd packages/browserExtension && yarn && yarn build-prod
          
      - uses: actions/upload-artifact@v3
        with:
          name: prod-extension
          path: packages/browserExtension/build-prod
